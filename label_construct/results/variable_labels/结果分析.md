## method预测
### 合理意见
210: 给出的修改意见是将“回归系数显著性与方向解读“改为“均值检验“，合理
468: 检验中的自由度计算，现在放在均值检验里了，给出的修改意见是改为“方差分析“，有一定合理性
469: pooled variance的计算，现在放在均值检验里了，给出的修改意见是改为“分散程度测度“，合理
### 不合理意见
126: 给出的修改意见是新增标准误计算类别，不必要
272: 关于relative risk的检验，现在放在均值检验里了，给出的修改意见是改为“独立性检验“，不合理，可以改为“风险比检验“
361: 给出的修改意见是将“期望“改为“分散程度测度“，不合理
548: 给出的修改意见是将“观测数据“改为“数据收集方法评估“，不合理，因为问题没有答案
### 遗漏的错误标签
522: 当前问题是 fisher exact test，没有检查出来。应该新开一类（其他），专门放一些不太常见的检验方法

### 第一次分析结果（前 6 条）
51: 本来只是绘制stem plot，但是llm把它当作了Y
114: 本来不应该包含data，但是llm把z score错判为data。需要调整prompt，明确告诉llm z score不是data。可以在prompt中加入类似“请注意，z score不是数据，而是一个统计指标”这样的说明。
151: 需要做两总体t检验，本来不区分两组的role，但是llm把其中一组的均值当作了X, 另外一组当作了Y。
180: 本来分析的对象是两列变量（X1,X2）的差值，但是gpt40把X2的role当作了Y

### 第二次分析结果
针对role的错误判断和data提取范围的错误判断，调整prompt后，结果如下：
#### role 判断不准
51: 本来只是绘制stem plot，但是llm把它当作了Y
151: 需要做两总体t检验，本来不区分两组的role，但是llm把其中一组当作了X, 另外一组当作了Y。
180: 本来分析的对象是两列变量（X1,X2）的差值，但是gpt40把两者的role当作了XY
210: 两总体t检验，但是两个总体的差别来源于一个变量的不同取值（如性别），本来不区分两组的role，但是llm把它们都当成了XY
272: 需要计算两个 group的risk并做假设检验，但是llm将两组的分子都当作了Y
346: role错判为Y，
> 问题：data结果被截断
#### value提取不完整
211: group的 value 不完整，只写了当前变量有两个取值，但是没有给出每个 sample 中的具体取值
#### 找错变量
319: 没有数据（纯文字），但是被错误提取
446: 未成功提取出处理对象的均值、方差、样本数等汇总量
468: 只提取了两个 group的sample size，但没有提取均值、方差等统计量
469: 只提取了两个 group的sample size和方差，但没有提取均值
473: 假设检验，未包含题目中的mu1_minus_mu2，错误包含了"z_{1-(0.05 / 2)}"

总结：prompt优化空间已经不大了，而且也加入了迭代机制。下一步可能需要尝试用更强的模型构建标签。